# Local Streamlit serving environment.
# Training-side packages (sentence-transformers etc.) live in Colab notebooks.

streamlit>=1.30
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pyarrow>=14.0
matplotlib>=3.7
joblib>=1.3
Pillow>=10.0

# LLM (added for final project)
groq>=0.4
